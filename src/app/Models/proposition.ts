import {User} from './User';
import {Stage} from './Stage';

export class proposition {
  private id_proposition: number;
  private propositionscanner: string;
  private user: User;
  public name:string;
  private type: string;
}
